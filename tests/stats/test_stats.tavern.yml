test_name: Статистика

marks:
  - usefixtures:
      - admin_token

stages:
  - name: "Получить обзор (Overview)"
    request:
      url: "{BASE_URL}/stats/overview"
      method: GET
      headers:
        Authorization: "Bearer {admin_token}"
    response:
      status_code: 200
      json:
        volume: !anything
        gmv: !anything
